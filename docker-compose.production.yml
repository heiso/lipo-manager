version: '3'

services:
  app:
    image: heiso/lipo-manager:latest
    ports:
      - ${APP_PORT}:3000
    links:
      - postgres
      - redis

  redis:
    image: redis:latest
  
  postgres:
    image: postgres:alpine
